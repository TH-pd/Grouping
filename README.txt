Data/members.txtにあるメンバーを2つのグループに分割します。
研究室のランチミーティングのグループ分けのために作成しました。

members.txtについて、
「・○○」が各グループ（学年）を表しており、その次の行から1行に1人ずつ名前が入ります。

pythonファイルを実行すると、logディレクトリ内に実行結果がtxt形式とcsv形式で保存されます。
csv形式は次回以降で同じ組み合わせが選ばれにくくするために使用されます。
